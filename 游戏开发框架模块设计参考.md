# 游戏开发框架模块设计参考

设计“游戏开发框架”时，最重要的问题不是“模块越多越好”，而是：

> 哪些能力具有跨项目复用价值、生命周期稳定、能提供统一规则，应该下沉到框架；哪些能力强依赖具体玩法，应该留在游戏业务层。

先给一个判断原则：

- 框架负责“所有游戏都会遇到的问题”。
- 游戏层负责“这个游戏为什么好玩”。
- 引擎负责“如何渲染、播放声音、进行物理计算”。
- 工具链负责“如何高效生产、检查和发布内容”。

这几层如果边界清楚，框架就会比较健康。

---

## 一、先区分：引擎、框架和游戏业务

以 Unity、Unreal、Godot 这类项目为例，可以这样划分：

```text
游戏业务层
├─ 战斗规则
├─ 角色技能
├─ 关卡机制
├─ 任务剧情
└─ 具体玩法

项目框架层
├─ 启动流程
├─ 模块生命周期
├─ UI 管理
├─ 资源管理
├─ 场景管理
├─ 配置与存档
├─ 网络通信
├─ 事件与消息
└─ 日志、调试、监控

引擎适配层
├─ Unity / Unreal / Godot API 封装
├─ 平台差异
├─ 文件系统
├─ 输入设备
└─ 渲染、音频、物理接口

底层引擎与平台
```

框架通常不应重新实现渲染器、物理引擎或完整动画系统，除非你是在开发真正的游戏引擎。

---

# 二、适合作为框架核心的能力

## 1. 应用启动与生命周期

这是框架最应该统一的部分之一。

可以包含：

- 游戏启动流程
- 环境初始化
- 配置加载
- 热更新检查
- 登录与鉴权
- 基础数据加载
- 首场景进入
- 前后台切换
- 暂停与恢复
- 退出与资源释放
- 模块初始化顺序
- 异常启动恢复

例如：

```text
Bootstrap
  → 初始化日志
  → 初始化本地配置
  → 初始化资源系统
  → 检查版本
  → 初始化网络
  → 登录
  → 加载玩家数据
  → 进入主界面
```

比较好的做法是定义明确的生命周期：

```csharp
public interface IGameModule
{
	void Initialize();
	void Start();
	void Update(float deltaTime);
	void Shutdown();
}
```

但不必让所有模块都强制实现 `Update`。大量空 `Update` 会带来不必要的调度开销和耦合，可以按需拆分接口：

```csharp
public interface IInitializable
{
	void Initialize();
}

public interface IUpdatable
{
	void Tick(float deltaTime);
}

public interface IDisposable
{
	void Dispose();
}
```

框架应解决：

- 谁创建模块
- 谁持有模块
- 初始化顺序是什么
- 初始化失败怎么办
- 模块间如何获得依赖
- 游戏退出时以什么顺序销毁

这是比“写一个单例管理器”更重要的部分。

---

## 2. 模块系统与依赖管理

框架可以提供一个轻量的模块注册机制。

例如：

```text
GameApp
├─ ResourceModule
├─ ConfigModule
├─ AudioModule
├─ UIModule
├─ SceneModule
├─ NetworkModule
└─ SaveModule
```

可支持：

- 模块注册
- 模块查询
- 模块依赖声明
- 初始化顺序计算
- 重复注册检测
- 循环依赖检测
- 模块状态检查
- 测试替身注入

这里有三种常见方案。

### 方案 A：Service Locator

```csharp
var audio = Services.Get<IAudioService>();
```

优点：

- 简单
- 学习成本低
- 游戏项目使用方便

缺点：

- 依赖是隐藏的
- 测试比较困难
- 容易退化成“全局对象仓库”

### 方案 B：依赖注入

```csharp
public BattleSystem(
	IAudioService audio,
	IConfigService config)
{
}
```

优点：

- 依赖明确
- 可测试性好
- 容易替换实现

缺点：

- 配置和理解成本更高
- 过度使用容易让简单项目复杂化

### 方案 C：显式组装

在入口直接创建并连接各模块：

```csharp
var resources = new ResourceService();
var audio = new AudioService(resources);
var ui = new UIService(resources);
```

优点：

- 最透明
- 没有容器魔法
- 很适合中小型游戏

缺点：

- 大项目的组装代码会变长

如果是自研框架，我通常更倾向于“显式组装为主，轻量容器为辅”，不要一开始就实现复杂的依赖注入框架。

---

## 3. 时间与更新调度

游戏中的时间并不只有一种。

框架可以统一：

- 帧时间
- 固定时间步
- 游戏逻辑时间
- UI 时间
- 不受暂停影响的真实时间
- 慢动作时间
- 服务器同步时间
- 可测试的虚拟时间
- 定时器与延迟任务

例如：

```text
RealTime       不受暂停和倍速影响
GameTime       受暂停和 TimeScale 影响
FixedTime      用于固定步长模拟
ServerTime     与服务器时间校准
UITime         UI 动画可以独立控制
```

定时器系统可以支持：

- 延迟调用
- 重复调用
- 取消
- 暂停
- 按 owner 批量取消
- 使用不同时间源
- 场景卸载自动清理

```csharp
timer.Delay(
	seconds: 2,
	callback: SpawnEnemy,
	owner: this,
	timeSource: GameTime);
```

特别需要避免“对象销毁了，定时回调还在执行”的问题。所有异步任务、事件监听、计时器最好都具备明确的所有权。

---

## 4. 事件、消息与信号系统

适合作为框架能力，但需要克制。

可以提供：

- 同步事件
- 类型化事件
- 一次性事件
- 带优先级监听
- 自动解绑
- 作用域事件
- 跨模块消息
- 请求—响应消息

例如：

```csharp
eventBus.Publish(new PlayerLevelChanged
{
	OldLevel = 10,
	NewLevel = 11
});
```

事件适合：

- 一个事实发生后，多个模块需要响应
- 发布者不应知道订阅者
- UI、音效、成就等旁路系统监听游戏状态变化

事件不适合：

- 强顺序业务流程
- 必须得到返回值的调用
- 核心战斗逻辑的每一步
- 高频、性能敏感的内部循环
- 用来逃避正常的函数调用和依赖关系

常见反模式是“所有东西都发事件”，最后调用链无法追踪。

比较稳妥的规则是：

> 命令使用直接调用，事实通知使用事件。

例如：

- `player.TakeDamage(10)`：直接调用，是命令。
- `PlayerDamaged`：事件，是已经发生的事实。

---

## 5. 异步任务系统

现代游戏开发中，异步能力几乎是基础设施。

可以统一：

- 资源异步加载
- 场景加载
- 网络请求
- UI 打开
- 下载更新
- 延迟与等待条件
- 取消任务
- 超时
- 重试
- 进度报告
- 错误传播
- 主线程切换
- 对象销毁时自动取消

关键不是仅仅提供 `async/await`，而是明确：

- 任务属于谁
- 任务什么时候取消
- 异常由谁处理
- 场景退出后回调是否还能运行
- 如何避免重复请求
- 多个请求是否共享同一个加载任务

建议提供统一的取消作用域：

```csharp
await resource.LoadAsync<CharacterPrefab>(
	path,
	cancellationToken: lifetime.Token);
```

对象销毁、窗口关闭或场景切换时，自动取消其持有的任务。

---

## 6. 资源管理

这是游戏框架中非常核心的模块。

可包含：

- 同步与异步加载
- 引用计数
- 资源缓存
- 依赖资源管理
- 实例化与释放
- 资源分组
- 资源标签
- 预加载
- 场景资源作用域
- 内存预算
- LRU 淘汰
- 弱引用缓存
- 远程资源下载
- 资源版本与校验
- 加载失败重试
- 资源泄漏检测

理想的资源接口不应该把具体引擎实现暴露给业务：

```csharp
var handle = await resources.LoadAsync<GameObject>("characters/hero");
var instance = handle.Instantiate();

handle.Release();
```

资源系统需要明确区分：

- 资源对象
- 实例对象
- 加载句柄
- 缓存所有权
- 使用者所有权

尤其要避免一种模糊设计：

```csharp
var prefab = Load("hero");
```

调用者不知道：

- 是否需要释放
- 什么时候释放
- 是否来自缓存
- 加载失败怎么办
- 场景切换后是否有效

资源框架的重要价值，是把资源生命周期变得可观察、可追踪。

---

## 7. 对象池

对象池可以作为框架提供的通用工具，但不应该默认所有对象都池化。

适合池化的对象：

- 子弹
- 特效
- 飘字
- 重复出现的敌人
- UI 列表项
- 网络消息缓冲区
- 临时集合

框架能力可以包括：

- 泛型对象池
- 引擎对象池
- 自动扩容
- 最大容量
- 预热
- 获取与归还回调
- 重复归还检测
- 泄漏检测
- 场景级对象池
- 全局对象池
- 调试统计

例如：

```csharp
var bullet = bulletPool.Rent();
bulletPool.Return(bullet);
```

需要注意：

- 池化会让生命周期更复杂。
- 重用对象必须重置全部状态。
- 不要因为担心 GC 就把所有东西塞进池。
- 是否池化应以性能分析结果为依据。

---

## 8. 场景、关卡与世界流程

框架可以管理：

- 场景加载与卸载
- 单场景或叠加场景
- Loading 页面
- 加载进度聚合
- 场景切换状态
- 场景资源作用域
- 场景服务初始化
- 关卡进入与退出
- 切换失败回退
- 防止重复切换
- 场景切换事务

例如：

```text
请求进入战斗
  → 禁止重复操作
  → 打开 Loading UI
  → 保存当前必要状态
  → 卸载旧场景
  → 加载战斗资源
  → 加载战斗场景
  → 初始化关卡
  → 玩家出生
  → 关闭 Loading UI
```

建议区分三个概念：

- Scene：引擎中的场景资源
- Level：一局或一个关卡的数据
- Game Flow：主菜单、战斗、结算等流程状态

不要让“加载一个 Scene”同时承担登录、加载数据、初始化战斗、打开 UI 等所有责任。

---

## 9. UI 框架

UI 管理往往是项目框架中工作量最大的部分之一。

可以提供：

- 窗口注册与创建
- 打开、关闭、隐藏
- UI 层级
- 页面、弹窗、提示分类
- 窗口栈
- 返回键处理
- 遮罩与点击穿透
- 窗口互斥
- 窗口缓存
- 异步加载
- UI 动画
- 安全区适配
- 分辨率适配
- 红点系统
- 通用列表
- 消息提示
- Loading
- 本地化文本刷新
- UI 生命周期
- UI 调试查看器

一种常见分层：

```text
Screen：全屏页面
Popup：弹窗
Overlay：常驻覆盖层
Toast：短暂提示
HUD：战斗信息
System：系统级加载、网络断开、错误提示
```

UI 生命周期可以类似：

```csharp
OnCreate()
OnBind()
OnOpen(args)
OnShow()
OnHide()
OnClose()
OnDestroy()
```

但生命周期不是越多越好。每个回调都必须有清晰、不可重叠的语义。

UI 框架最好还能解决数据绑定，不过不一定要直接实现庞大的 MVVM。可选方向：

### 简单 Presenter

- 容易理解
- 适合中小项目
- 手动刷新代码较多

### 响应式绑定

- 状态变化自动更新 UI
- 适合复杂界面
- 调试链路更长
- 容易产生订阅泄漏

### MVVM

- UI 与逻辑边界清楚
- 测试性较好
- 对简单游戏可能过重

框架最好提供基础机制，不强制所有页面采用同一种复杂架构。

---

## 10. 音频系统

音频很适合统一管理。

可以提供：

- BGM
- 环境音
- UI 音效
- 战斗音效
- 语音
- 音量分组
- 静音
- 淡入淡出
- BGM 切换
- 音效并发限制
- 相同音效冷却
- 音频资源加载与释放
- 3D 音效
- 音频对象池
- 暂停恢复
- 配置持久化

例如：

```csharp
audio.PlaySfx(SfxId.ButtonClick);
audio.PlayMusic(MusicId.Battle, fadeSeconds: 1.0f);
```

音频系统可提供“播放能力”，但“什么时候播放某个技能音效”属于游戏业务。

---

## 11. 输入系统

可以抽象：

- 键盘鼠标
- 手柄
- 触摸
- 多点触控
- 手势
- 虚拟摇杆
- 输入重映射
- 输入上下文
- 按键冲突
- UI 与游戏输入竞争
- 输入录制与回放
- 可访问性输入

推荐基于“动作”而不是具体按键：

```text
Jump
Attack
Interact
OpenMenu
Move
CameraLook
```

而不是在业务里到处判断：

```csharp
if (KeyCode.Space)
```

还可以使用输入上下文：

```text
GameplayContext
MenuContext
DialogueContext
VehicleContext
SpectatorContext
```

对战游戏还需要考虑：

- 输入帧编号
- 输入缓冲
- 连招宽限时间
- 网络预测
- 确定性回放

这些高级能力只有在项目确实需要时再实现。

---

## 12. 配置与数据表

框架可以统一管理：

- JSON、CSV、二进制配置
- 配置加载
- 类型转换
- ID 索引
- 关联校验
- 默认值
- 版本兼容
- 配置热重载
- 编辑器导出
- Excel 数据导入
- 多语言字段
- 配置差异检查
- 服务端与客户端共享协议
- 构建期验证

例如：

```csharp
var skill = configs.Skills.Get(skillId);
```

配置框架最有价值的部分往往不是“反序列化”，而是验证：

- ID 是否重复
- 引用的资源是否存在
- 外键是否有效
- 数值范围是否合理
- 枚举是否有效
- 必填字段是否为空
- 不同配置表之间是否矛盾

很多线上问题并不是代码 bug，而是错误配置没有在构建前被发现。

---

## 13. 存档系统

可以支持：

- 玩家设置
- 单机进度
- 多存档槽
- 自动存档
- 检查点
- 云存档
- 数据校验
- 原子写入
- 备份恢复
- 数据迁移
- 存档版本
- 压缩
- 可选加密
- 损坏检测

重要能力是版本迁移：

```text
Save V1
  → Migration V1ToV2
  → Save V2
  → Migration V2ToV3
  → Save V3
```

不要直接把整个运行时对象图序列化。更稳妥的办法是定义独立存档模型：

```csharp
class PlayerSaveData
{
	public int Version;
	public int Level;
	public List<int> UnlockedItems;
}
```

存档写入应尽量采用：

```text
写入临时文件
→ 校验
→ 替换正式文件
→ 保留上一份备份
```

这样可以降低断电或崩溃导致存档损坏的风险。

---

## 14. 网络通信

适合抽象的通用能力包括：

- HTTP 请求
- WebSocket / TCP / UDP 接入
- 请求序列号
- 请求—响应匹配
- 超时
- 取消
- 重试
- 心跳
- 重连
- 鉴权
- 消息序列化
- 协议版本
- 错误码统一处理
- 网络状态变化
- 流量统计
- 请求日志
- 模拟网络
- 延迟、丢包测试
- 幂等请求支持

网络框架负责“可靠地发消息和接消息”，业务层负责消息含义。

例如：

```csharp
var response = await network.SendAsync<LoginResponse>(
	new LoginRequest { Token = token },
	timeout: TimeSpan.FromSeconds(10));
```

不建议让网络层直接操作具体 UI：

```text
网络超时 → NetworkModule 直接打开错误弹窗
```

更好的方式是：

```text
网络层产生结构化错误
→ 流程层决定重试、退出还是提示
→ UI 层负责展示
```

---

## 15. 状态机与流程系统

框架可以提供通用状态机：

- 有限状态机 FSM
- 分层状态机 HFSM
- 异步状态切换
- 状态进入与退出
- 切换条件
- 状态历史
- 调试可视化

适合用在：

- 游戏主流程
- AI 行为
- 角色状态
- UI 页面状态
- 战斗阶段
- 登录流程

例如：

```text
Boot
→ Patch
→ Login
→ Lobby
→ LoadingBattle
→ Battle
→ Result
→ Lobby
```

不过，不建议做成“一套状态机统治所有系统”。角色状态、游戏流程和 AI 状态的需求很可能不同。框架可以共享基础接口，但允许不同实现。

---

## 16. 实体与组件模型

这部分要慎重，因为游戏引擎通常已有 GameObject、Actor、Node 或 ECS。

可选方式：

### 使用引擎对象模型

优点：

- 简单
- 与编辑器配合好
- 开发效率高

缺点：

- 逻辑容易依赖引擎对象
- 单元测试较难
- 大规模对象性能可能受限

### 自建轻量逻辑实体

```text
Entity
├─ AttributeComponent
├─ SkillComponent
├─ BuffComponent
└─ MovementComponent
```

优点：

- 逻辑与表现可以分离
- 适合服务器共用逻辑
- 测试更方便

缺点：

- 需要处理实体与引擎对象同步
- 生命周期复杂度增加

### ECS

优点：

- 批量数据处理性能好
- 适合大量同构实体
- 数据导向明确

缺点：

- 学习和调试成本较高
- 不适合所有玩法
- 容易为了架构而架构

框架可以提供实体生命周期、ID、组件查询、对象映射等基础能力，但不要在没有性能和玩法需求时强行上完整 ECS。

---

## 17. 属性、Buff、技能与战斗框架

这些能力是否下沉，要看框架目标。

如果你的框架专门服务 RPG、ARPG、卡牌或 MMO，那么可以包含：

- 属性系统
- 属性修饰器
- Buff 与 Debuff
- 技能释放流程
- 冷却
- 消耗
- 目标选择
- 伤害计算管线
- 效果系统
- 标签系统
- 战斗事件
- 仇恨
- 战斗日志
- 战斗回放

例如属性计算：

```text
基础值
→ 固定值加成
→ 百分比加成
→ 最终倍率
→ 上下限约束
```

但这不适合作为通用游戏框架的最底层模块，因为不同游戏的战斗规则差异很大。

可以把它设计成框架之上的“领域扩展包”：

```text
Core Framework
├─ 通用基础设施
└─ Gameplay Packages
   ├─ RPG Combat
   ├─ Card Battle
   ├─ Dialogue
   └─ Quest
```

---

## 18. AI 框架

可能包含：

- FSM
- 行为树
- Utility AI
- 黑板系统
- 感知系统
- 目标选择
- 寻路适配
- AI 调度与降频
- 调试可视化
- 行为回放

它更适合作为可选模块，而不是框架核心。

不同 AI 方案的适用场景：

| 方案 | 适合场景 | 主要问题 |
|---|---|---|
| FSM | 简单敌人、明确状态 | 状态多时容易爆炸 |
| 分层 FSM | 中等复杂角色 | 层级关系需要控制 |
| 行为树 | 流程化 AI | 数据共享和中断逻辑容易复杂 |
| Utility AI | 多目标动态决策 | 参数调试成本高 |
| GOAP | 自动规划 | 性能和可预测性更难控制 |

---

## 19. 相机系统

可以提供：

- 相机模式
- 跟随目标
- 平滑移动
- 镜头震动
- 镜头缩放
- 镜头边界
- 多目标构图
- 镜头切换
- 过场镜头
- 优先级
- 镜头效果叠加
- 分屏
- 相机调试工具

如果引擎已经有成熟相机方案，框架可以只负责上层调度，不要重复实现底层相机算法。

---

## 20. 本地化与可访问性

本地化适合成为框架基础能力：

- 文本翻译
- 参数化文本
- 复数规则
- 性别和语法变体
- 字体切换
- 语言包加载
- 图片本地化
- 音频本地化
- RTL 从右到左布局
- 运行时切换语言
- 缺失翻译检测
- 文本溢出检测

例如：

```csharp
localization.Get(
	"battle.damage",
	new { value = 100, target = "Goblin" });
```

可访问性可以包含：

- 字体大小
- 色盲模式
- 高对比度
- 按键重映射
- 长按替代连点
- 减少镜头震动
- 字幕
- 屏幕阅读提示
- 操作辅助

它们越晚加入，改造成本通常越高，因此基础接口最好早点考虑。

---

# 三、非常值得建设的开发与调试能力

很多框架只关注运行时代码，但真正提高团队效率的往往是工具。

## 1. 统一日志系统

可以支持：

- 日志等级
- 分类
- 标签
- 时间戳
- 帧号
- 对象上下文
- 文件输出
- 远程日志
- 日志过滤
- 结构化字段
- 发布版本裁剪
- 敏感信息脱敏

```csharp
logger.Info(
	category: "Network",
	message: "Request completed",
	fields: new { requestId, durationMs });
```

不要只封装一句 `Debug.Log`。日志系统的意义是可以定位问题，并控制生产环境的输出。

---

## 2. 游戏内调试面板

这是投入产出比很高的功能。

可以显示：

- FPS
- 内存
- Draw Call
- 网络延迟
- 当前场景
- 玩家位置
- 资源缓存
- 对象池状态
- 事件监听数量
- 当前任务
- 当前 Buff
- 服务端环境
- 日志查看
- 调试命令
- GM 命令

还可以提供命令：

```text
teleport 10 0 20
give_item 1001 99
set_level 20
reload_config
show_hitbox true
```

---

## 3. 性能监控与埋点

框架可统一：

- 方法耗时采样
- 加载耗时
- 首屏时间
- 场景切换时间
- 内存峰值
- 卡顿帧
- 网络失败率
- 崩溃上下文
- 用户行为事件
- 性能区间统计

建议区分：

- 技术遥测：性能、错误、资源。
- 产品埋点：用户行为、漏斗、留存。

不要把二者混成一个不可维护的接口。

---

## 4. 错误处理

可以统一定义错误模型：

```csharp
GameError
├─ Code
├─ Category
├─ Message
├─ UserMessageKey
├─ IsRetryable
├─ Cause
└─ Context
```

框架应决定：

- 哪些错误可以恢复
- 哪些需要重试
- 哪些需要退出当前流程
- 哪些必须上报
- 哪些只写日志
- 如何避免同一错误弹出几十次

错误处理不应只是：

```csharp
try
{
}
catch
{
}
```

吞掉异常往往比崩溃更难排查。

---

## 5. 资源和生命周期诊断

很值得提供：

- 未释放资源列表
- 资源引用来源
- 活跃异步任务
- 未取消计时器
- 事件订阅关系
- 未回收对象池对象
- 长时间存在的 UI
- 场景卸载后的残留对象
- 内存快照标签

游戏框架里最常见的问题之一，就是“谁还持有这个对象”。框架如果能回答这个问题，价值会很高。

---

## 6. 自动化测试支持

框架本身要做到可测试。

可支持：

- 模块单元测试
- 虚拟时间
- 模拟网络
- 内存资源提供器
- 临时存档
- 固定随机种子
- 输入录制与回放
- 战斗模拟
- 场景冒烟测试
- 配置验证
- 无界面运行

例如时间不应直接写死为引擎静态 API：

```csharp
public interface IClock
{
	double Now { get; }
}
```

测试时就可以使用：

```csharp
var clock = new FakeClock();
clock.Advance(5);
```

这会显著降低计时器、Buff、冷却、活动时间等系统的测试难度。

---

# 四、适合作为“可选扩展包”的模块

这些模块有复用价值，但不是所有游戏都需要：

- 任务系统
- 成就系统
- 新手引导
- 对话系统
- 剧情系统
- 红点系统
- 邮件系统
- 好友系统
- 公会系统
- 排行榜
- 商城
- 支付
- 活动系统
- 奖励系统
- 背包
- 装备
- 合成
- 抽卡
- 战斗通行证
- 日常任务
- 角色养成
- 地图与小地图
- 回放系统
- 录像系统
- Mod 系统
- 控制台
- 作弊检测
- 多人房间
- 匹配系统
- 服务器发现
- DLC 管理

比较好的组织方式是：

```text
Framework.Core
Framework.UI
Framework.Resources
Framework.Network
Framework.Save
Framework.Diagnostics

Gameplay.Quest
Gameplay.Dialogue
Gameplay.Inventory
Gameplay.Ability
Gameplay.AI
Gameplay.Tutorial
```

核心层保持小而稳定，玩法包按项目选择。

---

# 五、不建议直接塞进通用框架的内容

## 1. 具体玩法规则

例如：

- 攻击力如何计算
- 卡牌抽取规则
- 敌人掉落概率
- 玩家升级曲线
- 某关卡胜利条件
- 某个角色如何连招

这些属于领域逻辑。

## 2. 具体美术表现

例如：

- 某个技能播放哪个特效
- 某个按钮如何动画
- 某个 Boss 出场镜头
- 某个角色受击材质变化

框架可以提供播放机制，但具体组合应由业务或数据决定。

## 3. 为“可能的未来”设计的抽象

例如：

- 当前只有一个资源后端，却设计十层资源提供器。
- 当前没有联机，却提前开发完整网络同步框架。
- 当前只有一种 UI，却设计高度动态的插件式 UI 系统。
- 当前只有几十个单位，却上完整 ECS。
- 当前不支持 Mod，却从第一天建立脚本沙箱。

这些不是绝对错误，但必须有实际需求支撑。

---

# 六、判断某项能力是否应该进入框架

可以用下面这些问题判断。

## 应该进入框架的信号

- 多个项目都会用到。
- 多个模块需要遵守同一套规则。
- 生命周期和资源所有权需要统一。
- 需要跨平台适配。
- 需要统一监控、测试或错误处理。
- 业务层重复实现后容易出错。
- 接口相对稳定。
- 能够隐藏复杂的引擎或平台细节。

## 不应该进入框架的信号

- 只服务一个玩法。
- 需求还在频繁变化。
- 不同项目差异明显大于共性。
- 为了抽象而需要大量参数和回调。
- 业务开发者必须理解内部细节才能使用。
- 接口比直接调用更加复杂。
- 只有一个调用方。
- 尚未出现第二个真实用例。

一个很实用的规则：

> 第一次实现放在业务层；第二次观察共同点；第三次再考虑抽成框架。

并非所有东西都必须等三次，但这能减少过早抽象。

---

# 七、一个比较合理的框架分层

推荐把框架分成四层。

```text
┌─────────────────────────────┐
│ Gameplay                    │
│ 战斗、角色、任务、关卡、玩法 │
├─────────────────────────────┤
│ Feature Packages            │
│ 背包、对话、技能、AI、引导   │
├─────────────────────────────┤
│ Core Framework              │
│ 生命周期、资源、UI、配置、   │
│ 存档、网络、事件、时间、诊断 │
├─────────────────────────────┤
│ Engine / Platform Adapters  │
│ Unity、Unreal、移动端、PC    │
└─────────────────────────────┘
```

依赖方向应尽量单向：

```text
Gameplay → Feature → Core → Engine Adapter
```

Core 不应反向引用具体游戏代码。

如果确实需要业务提供实现，可以通过接口反转：

```csharp
public interface IPlayerIdentityProvider
{
	string GetPlayerId();
}
```

由游戏层注册实现，框架只依赖接口。

---

# 八、推荐的最小框架版本

如果从零开始，不建议一次做完几十个模块。

## 第一阶段：最小可用

优先实现：

1. 启动与模块生命周期
2. 日志和错误处理
3. 配置加载
4. 资源加载
5. 场景切换
6. UI 管理
7. 时间与异步取消
8. 存档
9. 基础调试面板

完成标准应是：能跑通一个完整的小型游戏流程。

```text
启动
→ 主菜单
→ 新游戏
→ 加载关卡
→ 游戏过程
→ 暂停
→ 结算
→ 保存
→ 返回菜单
```

## 第二阶段：生产能力

根据真实项目问题增加：

- 资源引用追踪
- 对象池
- 本地化
- 输入映射
- 网络封装
- 配置验证
- 性能指标
- 自动化构建
- 测试替身

## 第三阶段：领域扩展

按游戏类型选择：

- 技能和 Buff
- 任务
- 对话
- 背包
- AI
- 联机同步
- 回放
- 活动系统

---

# 九、框架设计中最容易踩的坑

## 1. Manager 泛滥

```text
GameManager
UIManager
AudioManager
PlayerManager
BattleManager
DataManager
SystemManager
ManagerManager
```

问题不在名字，而在于这些类通常：

- 是全局单例
- 职责不清
- 相互调用
- 隐藏依赖
- 无法测试
- 初始化顺序混乱

可以用 `Service`、`Repository`、`Controller`、`System` 等表达职责，但更重要的是让每个模块边界清楚。

## 2. 全局事件总线滥用

结果是：

- 不知道谁发了消息
- 不知道谁处理
- 执行顺序不清楚
- 业务链路难调试
- 忘记解绑导致泄漏

应该提供事件诊断和所有权机制，并限制使用场景。

## 3. 过度封装引擎

如果框架把引擎所有 API 原样包一层：

```csharp
FrameworkInstantiate()
FrameworkDestroy()
FrameworkSetActive()
```

但没有提供额外语义，这只是增加跳转层级。

只有满足以下至少一项时才值得封装：

- 需要替换实现
- 需要统一生命周期
- 需要记录和监控
- 需要跨平台
- 需要测试
- 需要控制访问方式

## 4. 生命周期不明确

最危险的问题通常是：

- 谁创建
- 谁释放
- 谁持有
- 什么时候失效
- 异步完成时对象是否还存在

框架接口最好能直接体现所有权，而不是依赖口头约定。

## 5. 框架依赖具体游戏

如果框架里出现：

```text
Player
Monster
Battle
Inventory
Quest
```

就要判断它到底是通用框架，还是某类游戏的业务基础库。

两者都可以，但应明确定位。

## 6. 为了“通用”增加大量配置

当一个接口有十几个布尔参数时：

```csharp
Open(
	cache: true,
	async: true,
	modal: false,
	reuse: true,
	preload: false,
	...);
```

往往说明抽象边界有问题。

## 7. 只设计正常流程

还需要设计：

- 资源加载失败
- 网络断开
- 存档损坏
- 配置缺失
- 重复初始化
- 重复关闭
- 对象提前销毁
- 场景切换被取消
- 异步任务超时
- 低内存
- 应用切后台
- 初始化部分成功

框架的质量通常体现在异常路径，而不是 Demo 的正常路径。

---

# 十、评价一个游戏框架是否优秀

一个好的框架未必功能最多，而是具备下面这些特点：

- 生命周期明确
- 依赖关系可追踪
- 资源所有权明确
- 异步操作可以取消
- 错误不会静默丢失
- 模块可以单独测试
- 调试时能够看到内部状态
- 不强迫简单功能采用复杂架构
- 业务代码不需要绕过框架
- 新成员能较快理解
- 删除框架模块不会牵动整个项目
- 不依赖大量全局单例
- 不把具体玩法写进通用核心
- 能支持项目当前需求，而不是想象中的所有需求

我比较倾向于把框架的目标概括为：

> 用统一、可观察、可测试的方式管理生命周期、依赖、资源和流程；不要试图替业务层决定游戏应该如何设计。

如果你准备实际做一个框架，下一步最好先确定它的定位，因为不同方向差异很大：

- Unity 单机游戏通用框架
- Unity 手游商业项目框架
- Unreal Gameplay 框架
- 联机游戏客户端框架
- 客户端与服务器共享逻辑框架
- RPG 专用玩法框架
- ECS / 数据导向框架
- 2D 独立游戏轻量框架

定位确定后，再决定模块优先级，会比先做一个“大而全”的框架稳妥得多。
