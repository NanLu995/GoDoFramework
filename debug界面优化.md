1.概览
    (1) warning 和 error 的数字 改成下划线这种能点击或者直接点击waring 或者 error 的框体，能跳转到控制台并完成筛选，先判断哪种方式比较好

2.系统

3.性能
    (1)渲染那个数据 是不带debug那个窗口的吗？能不能分开，主要是希望看到真正的游戏内容的渲染数据，就是核心不让debug窗口影响判断，这方面如何处理。
    关闭这个弹窗是不是影响最小的，第一个这个窗口打开能影响哪方面，关闭后，在出一个小的文本列表显示渲染数据？？类似cocos的做法，但是就是影响观感，这个地方研究讨论一下。

4.